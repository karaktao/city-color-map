# City Color Map - Docker Quick Start

## 1) Requirements
- Docker Desktop (Windows/Mac) or Docker Engine (Linux)
- docker compose

## 2) Setup env
Copy `.env.example` to `.env` and fill `MAPILLARY_TOKEN`.

## 3) Start
```bash
docker compose up -d --build
